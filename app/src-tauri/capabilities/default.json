{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面主窗口能力：core 默认集 + updater 默认集（自定义 command 无需额外声明）。updater 权限只对本地页面生效——远程网关页无 IPC（issue 07 约定），更新检查实际在 Rust 侧触发（src/updater.rs），此权限为本地页后续直连 JS API 预留。限桌面平台：updater 插件移动端未编入（issue 12 门控），移动端能力见 mobile.json。",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
