# 形态 C（本机自用）入口：Caddy -> 网关 -> dsh Host
#
#   https://localhost:8443  --reverse_proxy-->  127.0.0.1:3000（网关）
#
# - `localhost` 走 Caddy 本地 CA（tls internal）签发证书，无需公网域名。
#   浏览器首次使用须信任 Caddy 根证书（路径见 README「浏览器信任」）。
# - 端口 8443 而非 443：本机非 root 运行时无特权端口绑定权
#   （net.ipv4.ip_unprivileged_port_start=1024）。若以 root 或
#   CAP_NET_BIND_SERVICE 运行，把站点地址改回 `https://localhost` 即用 443。
# - WebSocket（/api/events.mux、/api/events.host）：reverse_proxy 自动
#   透传 Upgrade，无需额外配置。
# - 本形态仅限回环：站点只监听 localhost，网关与 dsh Host 也只绑 127.0.0.1。
{
	# 本机部署面不需要 admin API
	admin off
	# 非 root 无法绑 80 做 HTTP->HTTPS 跳转；本形态只经 https://localhost:8443 访问
	auto_https disable_redirects
}

https://localhost:8443 {
	tls internal
	reverse_proxy 127.0.0.1:3000
}
