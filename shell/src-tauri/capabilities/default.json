{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：core 默认集 + updater 默认集（自定义 command 无需额外声明）。updater 权限只对本地页面生效——远程网关页无 IPC（issue 07 约定），更新检查实际在 Rust 侧触发（src/updater.rs），此权限为本地页后续直连 JS API 预留。",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
